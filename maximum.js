// find the caracter that appears the most and total number of times it appears.

str = "Helllloow"

function mostAppears(str) {
    str = str.split("");
    max = str[0];
    for (char in str) {
        if (str[char] > max) {
            
        }
    }
}